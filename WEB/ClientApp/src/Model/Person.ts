import { observable, Subject, of, Observable, ReplaySubject, BehaviorSubject } from 'rxjs';

export class Person {
    constructor(){
   
    }
    idCard: string;  
    firstname: string;
    lastname: string;
}
